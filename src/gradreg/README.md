`gradregcae` defines a class of convolutional autoencoder trained by autoregressive prediction task.
The autoencoder is penalized by large norm of gradient of the loss function with respect to the inputs.